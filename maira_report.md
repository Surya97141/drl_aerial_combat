# MAIRA Research Report
**Generated:** 2026-03-06 09:52
**Project:** `/mnt/d/gbm_aviation_martin/drl_aerial_combat_research`
**Project Type:** REINFORCEMENT_LEARNING

---

## Experiment Gaps Detected

MAIRA identified the following untested experiment combinations:

- multiple_seeds
- ppo_vs_maddpg
- sweep_of_hyperparameters

---

## Dataset Architecture Decisions

| File | Type | Total | Split | Train | Val | Test |
|------|------|-------|-------|-------|-----|------|
| `ppo_logs/evaluations.npz` | timeseries | 25 | 70/15/15 | 17 | 4 | 4 |
| `data/analysis.json` | episodic | 10 | 70/15/15 | 7 | 1 | 2 |
| `data/replays.json` | episodic | 10 | 70/15/15 | 7 | 1 | 2 |

### Split Justifications

**ppo_logs/evaluations.npz**
- NPZ contains 25 evaluation checkpoints across training.
- Timeseries split — no shuffle. Earlier checkpoints = train, later = test.
- This tests whether MAIRA can predict performance trajectory from early training.

**data/analysis.json**
- Sorted by episode_id to preserve temporal order.
- Small dataset (10 episodes) — using 70/15/15 split to preserve test integrity.
- No random shuffle applied — episodic RL data has temporal dependency.
- Test set uses final episodes — simulates evaluating on unseen future behavior.

**data/replays.json**
- Sorted by episode_id to preserve temporal order.
- Small dataset (10 episodes) — using 70/15/15 split to preserve test integrity.
- No random shuffle applied — episodic RL data has temporal dependency.
- Test set uses final episodes — simulates evaluating on unseen future behavior.

---

## AI Research Advisor Output

**1. DIAGNOSIS**
The current state of this research is that it has made significant progress in implementing reinforcement learning algorithms, specifically PPO, as evidenced by the presence of `ppo_logs` and `baseline` model files. However, there are gaps in the experiments, including the lack of multiple seeds, comparison between PPO and MADDPG, and hyperparameter sweeps. The existing datasets, such as `evaluations.npz` and `analysis.json`, provide a foundation for further analysis and improvement.

**2. TOP 3 NEXT EXPERIMENTS**
- Experiment 1: **Multiple Seeds Evaluation** — Run the PPO algorithm with multiple random seeds (e.g., 5-10 seeds) to evaluate the robustness and variability of the results, as indicated by the `multiple_seeds` gap. This matters because it will help to assess the reliability of the model's performance and reduce the impact of random initialization on the results.
- Experiment 2: **PPO vs MADDPG Comparison** — Compare the performance of PPO and MADDPG algorithms, as suggested by the `ppo_vs_maddpg` gap, to determine which algorithm is more suitable for the task. This comparison will provide insights into the strengths and weaknesses of each algorithm and help to choose the best approach for the research.
- Experiment 3: **Hyperparameter Sweep** — Perform a sweep of hyperparameters, as indicated by the `sweep_of_hyperparameters` gap, to identify the optimal hyperparameters for the PPO algorithm. This matters because tuning hyperparameters can significantly improve the model's performance, and a systematic sweep will help to find the best combination of hyperparameters.

**3. REWARD/OBSERVATION FIX**
One specific code-level change to improve performance is to modify the reward function in the `analysis.json` file to include a penalty term for `failure_mode` and `worst_timestep`. For example, the new reward function could be `total_reward - 0.1 * failure_mode - 0.05 * worst_timestep`. This change will help to discourage the model from failing and experiencing worst-case timesteps, which will lead to more stable and effective learning. Specifically, this modification can be made in the `reward_trend` calculation in the `analysis.json` file, which currently contains 10 records with episode information.

---

*Generated by MAIRA — ML Agentic Intelligence for Research Automation*
*Zero instrumentation required. Point at any ML project folder.*